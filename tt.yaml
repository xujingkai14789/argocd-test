# myapp-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-gitlab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp-gitlab
  template:
    metadata:
      labels:
        app: myapp-gitlab
    spec:
      containers:
      - image: myapp-gitlab-7b99cffb45-5p7mn
        name: myapp-gitlab
        ports:
        - containerPort: 80
